version: '3.8'

services:
  # ============================================
  # FRONTEND - React + Vite
  # ============================================
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:3001/api/v1
    networks:
      - erp-network

  # ============================================
  # BACKEND - Node.js + Express
  # ============================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://erp_user:erp_password@postgres:5432/erp_lite
      - JWT_SECRET=change-this-secret-in-production-min-32-chars
      - FRONTEND_URL=http://localhost
      - PORT=3001
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - erp-network
    restart: unless-stopped

  # ============================================
  # DATABASE - PostgreSQL
  # ============================================
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=erp_user
      - POSTGRES_PASSWORD=erp_password
      - POSTGRES_DB=erp_lite
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U erp_user -d erp_lite"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - erp-network

  # ============================================
  # PGADMIN - Interface de administração (dev only)
  # ============================================
  pgadmin:
    image: dpage/pgadmin4:latest
    ports:
      - "5050:80"
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@erplite.com.br
      - PGADMIN_DEFAULT_PASSWORD=admin123
    depends_on:
      - postgres
    networks:
      - erp-network
    profiles:
      - tools

volumes:
  postgres_data:

networks:
  erp-network:
    driver: bridge
